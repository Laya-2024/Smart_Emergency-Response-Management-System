async function load(){const r=await fetch('/api/v1/shelters'),s=await r.json();document.querySelector('#shelters').textContent=s.map(x=>`${x.name}: ${x.capacityAvailable} spaces`).join('\n')||'No shelter data available.'}navigator.geolocation?.getCurrentPosition(p=>{document.querySelector('#relief').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.target);await fetch('/api/v1/relief-requests',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({requestType:f.get('requestType'),peopleCount:+f.get('peopleCount'),description:f.get('description'),latitude:p.coords.latitude,longitude:p.coords.longitude})});alert('Relief request submitted.')}});document.querySelector('#donation').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.target);await fetch('/api/v1/donations',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({donationType:f.get('donationType'),amount:f.get('amount')||null,currency:f.get('currency'),itemDescription:f.get('itemDescription')})});alert('Donation pledge recorded. Thanks for helping.')} ;load();
