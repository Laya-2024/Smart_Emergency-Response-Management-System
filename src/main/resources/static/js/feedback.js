document.querySelector('#feedback').addEventListener('submit',async e=>{e.preventDefault();const f=new FormData(e.target),r=await fetch(`/api/v1/emergencies/${f.get('emergencyId')}/feedback`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({rating:+f.get('rating'),comments:f.get('comments')})});document.querySelector('#message').textContent=r.ok?'Thank you. Your feedback helps improve emergency response.':'Feedback is available only after resolution, once per emergency.'});
